{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lerna": "lerna",
    "react-app": "lerna exec --scope react-app -- ",
    "react-app:run": "lerna run --scope react-app",
    "react-app:add": "lerna exec --scope react-app -- yarn add",
    "react-app:remove": "lerna exec --scope react-app -- yarn remove",
    "nest-app": "lerna exec --scope nest-app -- ",
    "nest-app:run": "lerna run --scope nest-app",
    "nest-app:add": "lerna exec --scope nest-app -- yarn add",
    "nest-app:remove": "lerna exec --scope nest-app -- yarn remove"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.2.3"
  }
}
